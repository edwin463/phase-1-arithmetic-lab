// Multiply two numbers whose result equals 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;  // Result: 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1;  // Random integer between 1 and 100

// Calculate remainder of 16 divided by 6
const num3 = 16;
const num4 = 6;
const mod = num3 % num4;  // Remainder is 4

// Find the maximum number in a set, which includes 20
const max = Math.max(1, 5, 10, 15, 20);  // Maximum number is 20
